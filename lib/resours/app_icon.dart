import 'package:e_commerce_mobile_app/resours/app_color.dart';
import 'package:flutter/material.dart';

abstract class AppIcons {
  static const drawerIcon = Icon(Icons.menu);
  static const searchIcon = Icon(Icons.search);
  static const cardIconStar = Icon(Icons.star);
  static const cardIconStarAmber = Icon(Icons.star, color: AppColors.cardIconStarColor);
}
